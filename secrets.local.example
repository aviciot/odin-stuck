# the-M local secret file
# Copy to secrets.local, set your own passphrase, never commit secrets.local
#
# Generate a good passphrase:
#   Linux/Mac: openssl rand -hex 32
#   Windows:   python -c "import secrets; print(secrets.token_hex(32))"
#
THE_M_MASTER_SECRET=replace-this-with-a-strong-random-passphrase
