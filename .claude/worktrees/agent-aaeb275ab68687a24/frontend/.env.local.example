# Odin frontend — local dev
# Copy to .env.local and adjust if running outside Docker

# Auth service URL (proxied by Next.js rewrites in prod)
NEXT_PUBLIC_AUTH_URL=http://localhost:8701/auth

# Bridge API URL (proxied by Next.js rewrites in prod)
NEXT_PUBLIC_API_URL=http://localhost:8001/api/v1
